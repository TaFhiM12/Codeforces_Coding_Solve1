#include<bits/stdc++.h>
using namespace std ;
#define nl "\n" 
#define int long long
#define optimize() ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define fraction(n) cout << fixed << setprecision(n);

int32_t main()
{
    optimize();
    fraction(10);
    double n , l ; cin >> n >> l ;
    vector<int> v(n);
    for(int i=0;i<n;i++) cin>> v[i] ;
    for()
    












    // v.push_back(l);
    // sort(v.begin(),v.end());
    // if(v[0] != 0 && v[n-1] != l){
    //     double mx1 =max(double(v[0]-0) , double(l - v[n-1]) ) ;
    //     double mx = 0 ;
    //     for(int i=1;i<v.size();i++)
    //     {
    //         mx = max(mx,double(v[i]-v[i-1])) ;
    //     }
    //     if(mx/2.0 > mx1) cout << mx/2.0 << nl;
    //     else cout << mx1 << nl ;
        
    // }
    // else if(v[0] != 0 && v[n-1] == l){
    //     double mx1 = double(v[0]-0) ;
    //     double mx = 0 ;
    //     for(int i=1;i<v.size();i++)
    //     {
    //         mx = max(mx,double(v[i]-v[i-1])) ;
    //     }
    //     if(mx/2.0 > mx1) cout << mx/2.0 << nl;
    //     else cout << mx1 << nl ;
        
    // }
    // else if(v[0] == 0 && v[n-1] != l){
    //     double mx1 = double(l - v[n-1]) ;
    //     double mx = 0 ;
    //     for(int i=1;i<v.size();i++)
    //     {
    //         mx = max(mx,double(v[i]-v[i-1])) ;
    //     }
    //     if(mx/2.0 > mx1) cout << mx/2.0 << nl;
    //     else cout << mx1 << nl ;
        
    // }
    // else {
    //     double mx = 0  ;
    //     for(int i=1;i<v.size();i++)
    //     {
    //         mx = max(mx,double(v[i]-v[i-1])) ;
    //     }
    //     cout << mx / 2.0 << nl ;
    // }
    
    return 0;
}