#include<bits/stdc++.h>
using namespace std;
#define int long long
#define nl '\n' 
#ifndef ONLINE_JUDGE
#define dd(...) cerr << "[" << #__VA_ARGS__ << "]:", debug_out(__VA_ARGS__);
#else
#define dd(x) 46
#endif

void solve() {

    int n , k , w ;
    cin >> n >> k >> w ;
    for(int i=1;i<)
}
signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    #ifndef ONLINE_JUDGE
        freopen("inputf.in","r",stdin);
        freopen("outputf.in","w",stdout);
        freopen("errorf.in","w",stderr);
    #endif
    int tc = 1;
    // cin >> tc;

    for(int i = 1; i <= tc; i++) {
        solve();
    }
    return 0;
}